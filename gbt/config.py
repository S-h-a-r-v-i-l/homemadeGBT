# Hyperparameter definitions for the GBT model (tree count, depth, learning rate, binning, loss, regularization).

tree_count = 100
max_depth = 2
learning_rate = 0.1
bin_count = 10

gamma = 0.1
lambda_ = 1.0
